# Project: watch-price-tracker（腕表二级市场价格走势）

零构建静态站，三语（zh/en/de）。追踪 7 款腕表在二级市场的季度价格走势历史。

## Commands
- 无构建/测试命令
- 本地预览：共享配置 `C:\Users\junpi\.claude\.claude\launch.json`

## 数据来源与性质（改 data.js 前必读）
`data.js` 里 `sampleData: false`，2024-01→2026-07 季度 CHF 价格序列，是**基于公开二手行情平台
（WatchCharts 等）数据估算、手动平滑处理的结果，不是精确成交记录**——每款表的 `note` 字段
（zh/en/de）都披露了这个数据来源性质，改动时保持这个披露，不要删掉或弱化措辞。

**如果用户（本人是奢侈腕表零售从业者）提供真实观察到的成交价，优先用真实数据覆盖估算值。**

## 已知的历史修正（如果用户质疑这两个参考号，是有原因的，别悄悄改回去）
- Patek Nautilus：曾错误标注为 `5711/1A-014`（实际是橄榄绿盘），已更正为 `5711/1A-010`（真正的蓝盘款），价格也相应大幅下调（约 CHF 70-106k，不是旧值的 110-145k）
- Cartier Santos "Medium"：曾错误标注 `WSSA0018`（实际是大号款），已更正为 `WSSA0029`

## 明确禁止的事
- 不要在没有真实数据支撑的情况下，把 `note` 字段里"这是估算数据不是精确成交记录"的披露语言删掉或改弱
- 不要在没有确认的情况下把上面两个已修正的参考号改回旧值

## Claude 工作方式
- **这个文件夹经常有另一个 Claude Code session/dev-server 并发在跑**——编辑或提交前先 `git status` 确认没有别的会话的未提交改动

## 部署流程
- 改完直接 commit + push 到 `main`
- Commit 作者身份：`Junping Koch <junping.koch@gmail.com>`，仓库单独设置

## 持续维护
每次你需要重复纠正 Claude 同一件事三次以上，就把结论补进这个文件对应章节。
